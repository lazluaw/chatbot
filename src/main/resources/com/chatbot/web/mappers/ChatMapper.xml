<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatbot.web.mappers.ChatMapper">
    <insert id="insertData" parameterType="chat"
            useGeneratedKeys="true" keyProperty="id">
        insert into chat (user_code, chat_kind, chat_body, insert_date)
        values (100020001, #{chatKind}, #{chatBody}, now())
    </insert>

    <update id="updateData" parameterType="chat">
        update chat set
        chat_kind = #{chatKind},
        chat_body = #{chatBody},
        update_date = now()
        where chat_id = 1
    </update>

    <delete id="deleteData" parameterType="chat">
        delete from chat where chat_id = #{chatId}
    </delete>
</mapper>